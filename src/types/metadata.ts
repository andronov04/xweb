export interface IAssetMetadata {
  name: string;
  description: string;
  tags: string[];
  artifactUri: string;
  displayUri: string;
  thumbnailUri: string;
  symbol: string;
  decimals: number;
  version: string;
}
